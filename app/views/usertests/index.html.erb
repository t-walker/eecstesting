<div class="row">
  <div class="col-md-6">
    <h2>Test Versions <small>Five Most Recent</small></h2>
    <%= render partial: "testversions/index", locals: { testversions: @testversions }  %>
    </div>
  <div class="col-md-6">
    <h2>Test Submissions <small>Five Most Recent</small></h2>
    <table class="table" style="color:black;">
      <tr>
        <th>Student's Name</th>
        <th>Score</th>
        <th>Date Taken</th>
        <th>View Test</th>
      </tr>
    <% @usertests.each do |ut| %>
      <% @user = User.find_by_id(ut.user_id) %>
      <tr>
        <td><%= @user.firstname %> <%= @user.lastname %></td>
        <td><%= ut.score %></td>
        <td><%= ut.created_at.to_formatted_s(:long) %></td>
        <td><%= button_to "View Test", usertest_path(ut.id), class: "btn btn-default btn-block", method: :get %></td>
      </tr>
      <% end %>
    </table>
    <%= link_to 'Download All as CSV', usertests_path(format: :csv) %>
  </div>
</div>
